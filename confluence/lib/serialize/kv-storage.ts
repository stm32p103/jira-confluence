/* ----------------------------------------------------------------------------
 * Key-Valueを保存する先のインターフェース
 * TODO 他ユーザによる更新を扱えるようにする。
 * ------------------------------------------------------------------------- */
export interface KVStorage {
  save( kv: { [key: string ]: any } ): Promise<void>;
  load(): Promise<{ [key: string ]: any }>;
}
